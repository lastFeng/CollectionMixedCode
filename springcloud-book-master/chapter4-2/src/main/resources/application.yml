# 自定义属性
my:
  name: forezp
  age: 12
  number:  ${random.int}
  uuid: ${random.uuid}
  max: ${random.int(10)}
  value: ${random.value}
  greeting: hi,i'm  ${my.name}

# 启动的profiles文件是哪一个
spring:
  profiles:
    active: dev
