server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/wsp?serverTimezone=UTC
spring.datasource.username=wsp
spring.datasource.password=Abcd123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# 连接池设置
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
# 如果是多连接池的话，就需要有多个连接的JavaBean的配置，而不是在这里配置
# 连接池补充设置
# Mybatis 设置
# 别名扫描，可以直接使用小写的类名就能扫描到相应的类
#mybatis.type-aliases-package=com.springboot.springshiromybatis.domain
# Mapper xml的路径
mybatis.mapper-locations=classpath:mapper/*.xml